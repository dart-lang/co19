/*
 * Copyright (c) 2013, the Dart project authors.  Please see the AUTHORS file
 * for details. All rights reserved. Use of this source code is governed by a
 * BSD-style license that can be found in the LICENSE file.
 */
/**
 * @assertion Parsing XHTML: Node's node document must be set to that of the
 * element to which it will be appended.
 * @needsreview
 */

import 'dart:html';
import "../../../../Utils/expect.dart";
import '../testcommon.dart';

main() {
  
  /*test('Test empty template', () {
    var doc = newXHtmlDocument();
     
    doc.body = doc.createElement('body');
    doc.body.innerHtml = '<template id="tmpl"></template>';

    var template = doc.querySelector('#tmpl');

    Expect.isNotNull(template,
        'Template element should not be null');

    Expect.isNotNull(template.content,
        'Content attribute of template element should not be null');

    Expect.equals(doc.body.ownerDocument, template.ownerDocument,
        'Wrong template node owner document');

    Expect.equals(doc, template.content.ownerDocument,
        'Wrong template content owner document');
  });*/
}